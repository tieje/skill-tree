const SideBarItemShortcutKey = ({ shortcutKey }: { shortcutKey: string }) => {
    return (
        <span className='opacity-50 text-lg text-center'>
            edit shortcut: {shortcutKey} key
        </span>
    )
}
export default SideBarItemShortcutKey
